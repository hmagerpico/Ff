name: blank

on:
  push:

jobs:
  neither:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: ["invest", "swift", "hard", "spoil", "hell"]
        domestic: ["laundry", "instant", "cricket", "contempt"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar eZ
